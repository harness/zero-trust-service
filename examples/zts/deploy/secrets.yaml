# SCM tokens — replace empty strings with real values.
# Consider gitignoring this file after filling in tokens.
apiVersion: v1
kind: Secret
metadata:
  name: zero-trust-service-secrets
  namespace: zero-trust-service
  labels:
    app.kubernetes.io/name: zero-trust-service
type: Opaque
stringData:
  GITHUB_TOKEN: ""
  GITLAB_TOKEN: ""
  HARNESS_CODE_TOKEN: ""
  ZTS_WEBHOOK_TOKEN: ""
